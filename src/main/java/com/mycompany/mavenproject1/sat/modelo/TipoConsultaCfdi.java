package com.mycompany.mavenproject1.sat.modelo;

/**
 * Perspectiva desde la que se consulta al RFC titular de la e.firma:
 * como emisor de los CFDI o como receptor de los CFDI.
 */
public enum TipoConsultaCfdi {
    EMITIDOS,
    RECIBIDOS
}
